-Rewrite serialization to only serialize entities.
-Add remove components.
-Drag and drop child parent 

-UI render target (res same to the main render target).

-add setting window.
	-set backbuffer width and height.
	-set vsync